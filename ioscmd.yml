--- 
- name: "Test IOS command."
  hosts: all
  gather_facts: true
  connection: network_cli
  
  tasks:
  - name: "show ip interface brief"
    cisco.ios.ios_command: 
      commands:
        - "show ip int brief"
